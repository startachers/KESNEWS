# Phase 9 자동화·운영 안정화 회귀 기록 — 2026-07-16

- 범위: DB 백업·복구, launchd, 단일 인스턴스, 로그 회전, 운영 상태, 병행운영 문서
- 불변 조건: Phase 8 최종 snapshot/HTML, 수동 기사 상태, 담당자 수정 요약, 마지막 정상 AI 결과

## 자동 검증

```text
.venv/bin/python -m pytest -q       → 110 passed
.venv/bin/ruff check .              → 통과
python compileall (backend/scripts) → 통과
bash -n (*.command)                 → 통과
node --check (frontend/js 전체)     → 통과
git diff --check                    → 통과
```

## 실제 HTTP 격리 검증

운영 DB와 분리한 `/private/tmp` DB와 127.0.0.1:8791 서버를 사용했다.

- [x] health의 서비스 식별자, instance ID, DB 연결·무결성 정상
- [x] operations status의 DB 무결성·최근 백업·수집 상태 응답
- [x] `/` 정적 화면 200과 신규 Console 검증 대상 JS syntax 통과
- [x] finalize 뒤 DB backup, schemaVersion 4 JSON, version 1 HTML 생성
- [x] `/report/2026-09-10` 최종본 200과 읽기 전용 문구 확인
- [x] 백업 파일이 WAL 보조 파일 없이 단일 `.db`로 생성되도록 보정

## 수동·운영 확인

- 브라우저 자동화 실행 도구가 세션에 노출되지 않아 클릭·인쇄 미리보기는 수행하지 못했다.
- 로컬 8787에는 사용자가 이전 코드로 실행한 서버가 있어 중지하지 않았다. 새 시작 스크립트는
  서비스 식별자가 없는 기존 health를 정상 Phase 9 서버로 오인하지 않고 포트 점유 안내를 낸다.
- launchd 항목은 구현만 했고 사용자 Mac에 자동 설치하지 않았다. 설치·재부팅·2시간 실운영은
  `docs/PARALLEL_OPERATION_CHECKLIST.md`에서 확인한다.

## 남은 운영 위험

- Mac 잠자기·종료 구간의 연합뉴스 RSS는 소급 수집을 보장하지 않는다.
- Google 뉴스 중계 URL의 canonical 해석 한계(LEG-011)는 유지된다.
- 실제 1~2주 병행운영의 데이터 손실·중대 누락 0건 판정 전에는 기존 업무 절차를 종료하지 않는다.
