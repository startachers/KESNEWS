# CEO 미리보기 AI 기사 본문 요약 회귀 기록 — 2026-07-22

## 변경 범위

- 작업본 CEO 미리보기에 `AI 본문 요약` 버튼 추가
- 현재 선정 기사 본문을 로컬 Ollama가 기사별 최대 3줄 분량·180자 이내로 요약
- 카드 제목에 이미 나온 사건·주체·결과는 빼고 원인·배경·수치·진행 상황·영향부터 이어서 작성
- 제목을 되풀이한 첫 문장을 자동 검출해 해당 기사만 한 번 교정하고, 재검사 실패 시 적용 거부
- 기사 ID 누락·중복·빈 요약을 거부하고 기존 본문을 유지
- 다른 AI 실행과 동시 실행을 막고 Ollama 오프라인 오류를 미리보기에 표시
- 성공한 요약은 현재 미리보기 DOM과 이어지는 인쇄·PDF에만 반영

기사 원본, 작업본 revision, 담당자 수정값, AI 분석 실행, 최종 snapshot은 변경하지 않는다.
최종 보고 HTML에는 버튼과 요약 실행 스크립트를 넣지 않는다.

## 자동 검증

- [x] `.venv/bin/python -m pytest -q` — 388 passed
- [x] `.venv/bin/ruff check .` — 통과
- [x] `git diff --check` — 통과
- [x] 정상 응답의 기사 ID별 본문 교체 데이터와 작업본 revision 불변 확인
- [x] 기사 누락 AI 응답이 `AI_SCHEMA_INVALID`로 거부되는지 확인
- [x] 미리보기에만 버튼이 있고 최종 보고에는 없는지 확인

## `MANUAL_REGRESSION_CHECKLIST.md` 관련 결과

- [x] 기존 기사 선택 상태와 작업본 revision이 요약 요청 전후 동일함을 API로 확인
- [x] 기존 중요 표시·메모·요약·snapshot 저장 경로를 변경하지 않음을 코드와 전체 테스트로 확인
- [x] 운영일 선정 기사 12건을 `gemma4:12b`로 실제 요약 — 12건 ID 일치, 약 35초, revision 114 유지
- [x] 같은 운영일 12건으로 제목 중복 방지 재검증 — 12건 모두 중복 검사 통과
- [x] 제목의 단순 재서술은 검출하고 제목에 없는 후속 사실은 허용하는 단위 테스트 통과
- [x] 제목 중복 항목만 2차 교정해 최종 결과로 교체하는 통합 테스트 통과
- [ ] 실제 브라우저의 버튼 클릭, Console 신규 오류, 인쇄 미리보기 2페이지 확인
- [ ] 기사 선택·해제, 중요 표시, 기사 메모, 날짜 변경, 요약 직접 수정, JSON·CSV 내보내기

현재 세션에는 로컬 브라우저 제어 연결이 없어 실제 클릭·인쇄 검증은 수행하지 못했다. 미실행 항목은
운영 브라우저에서 `/preview/{date}`를 열고 `AI 본문 요약`을 눌러 상태 문구와 2페이지 카드 본문,
인쇄 미리보기의 2쪽 유지를 확인해야 한다.

## 남은 위험

- 요약 품질과 생성 시간은 설치된 Ollama 모델 및 기사 본문 품질에 좌우된다.
- 결과는 의도적으로 미리보기에만 유지되므로 페이지를 새로고침하면 원래 본문 표시로 돌아간다.
- 180자 제한과 3줄 CSS 제한을 함께 적용하지만, 한글·숫자 폭에 따라 마지막 문장의 일부가 화면에서
  잘릴 수 있으므로 실제 운영 기사 12건으로 육안 확인이 필요하다.
