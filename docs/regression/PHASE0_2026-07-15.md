# Phase 0 수동 회귀 기준선 기록 — 2026-07-15

- 검증자: 담당자 직접 수행
- 검증 대상: `legacy/kesco_media_briefing_original.html` (기준선 커밋 82a69a3과 해시 동일 확인)
- 실행 방식: `file://` + `?noauto` (자동 네트워크 검색 차단)
- 체크리스트: `docs/MANUAL_REGRESSION_CHECKLIST.md`

## 확인 완료 항목

### 사전 준비
- [x] `.DS_Store` 없음
- [x] `.gitignore` 적용, `git status` 정상 (main = origin/main, 변경 없음)
- [x] 원본 HTML 미수정 (커밋본과 해시 동일)
- [x] `?noauto`로 기준 화면 열기
- [x] 개발자도구 Console 열기

### 초기 화면
- [x] 원본이 정상 표시됨
- [x] 기존 저장본 108건 정상 로드

### 기사 목록
- [x] 검색어 필터 정상: "양세찬" 입력 시 108건 → 9건, 테스트 후 검색어 제거
- [x] 검색 설정 창(설정 modal) 정상 작동

### 데이터 보존
- [x] 저장 데이터(localStorage)와 저장소 파일을 수정하지 않음

## Console 기준선 오류 (원본의 현재 동작 — 고치지 않고 기록)

Phase 1 분리본에서도 **동일하게** 나타나야 한다. 사라지거나 늘어나면 차이로 기록한다.

1. 로컬 AI 서버 `127.0.0.1:8787` 미실행으로 인한 연결 오류
   - AI 도우미(원클릭 실행 파일) 없이 열었을 때의 정상적인 오프라인 상태
2. `file://` 보안 origin 관련 오류
   - 원본은 `file://`로 열리고 분리본은 `http://` 정적 서버로 열리므로, 이 오류는 분리본에서 나타나지 않을 수 있음. 이는 실행 환경 차이이며 회귀가 아님 (체크리스트 사전 준비 항목 참조)

## 미수행 항목 (기존 저장 데이터를 변경하므로 백업 확보 후 수행)

- [ ] 기사 선택·해제 저장 (체크리스트 4장)
- [ ] 중요 표시·기사 메모 저장 (4장)
- [ ] 새로고침 후 상태 유지, 휴지통 동작 (4장)
- [ ] 브리핑 입력·요약 수정·날짜 변경 (5장)
- [ ] 설정 저장·기본값 복원 (6장)
- [ ] 네트워크 수집 실행 (6장 — `?noauto` 해제 필요)
- [ ] AI 관련 항목 (7장 — AI 도우미 실행 환경 필요)
- [ ] JSON·CSV 내보내기/가져오기 (8장 — 내보내기는 안전, 가져오기는 백업 후)
- [ ] 인쇄 미리보기 (9장 — 읽기 전용이므로 언제든 수행 가능)

## 미수행 항목 처리 계획

1. 앱의 **JSON 내보내기**를 먼저 실행해 백업 파일을 확보한다 (읽기 전용, 안전).
2. 브라우저 Console에서 localStorage 전체를 추가 백업한다.
3. 그 후 편집 상태·가져오기 항목을 수행하고, 필요 시 백업으로 원복한다.
4. 나머지 항목은 Phase 1 분리본 완성 후 **원본과 분리본을 나란히 비교**하면서 함께 수행해도 된다. 단, 수행 결과를 이 폴더에 날짜별 기록으로 남긴다.

## 결론

- Phase 0 기준선 커밋(82a69a3)과 안전 항목 검증 완료.
- 데이터 변경 항목이 남아 있으므로 **Phase 0 전체 완료로 기록하지 않는다**.
- Phase 1 착수는 가능하다. 분리본 회귀 비교 시 이 문서의 Console 기준선 2건과 미수행 항목을 함께 검증한다.
