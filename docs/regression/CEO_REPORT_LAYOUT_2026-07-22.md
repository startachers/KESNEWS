# CEO 보고 레이아웃 개선 회귀 기록 — 2026-07-22

## 변경 범위

- CEO 미리보기·최종 보고 상단에 `기사 중요도순` 전환 버튼 추가
- 중요도 정렬 기준: 중요 표시, 위험도, 자동 우선도 점수, 기존 편집순
- 1페이지 일반 분석 본문은 기상 특이사항과 같은 14px 일반체로 통일
- `오늘 한줄`의 `CEO VIEW` 라벨을 제거하고 본문은 10% 큰 15.4px 볼드체로 조정
- 2페이지 `관련기사` 상단을 밝고 낮은 별첨 헤더로 축소하고 남색 상단선·청록 측면선으로
  1페이지와 다른 위계를 부여
- 관련기사의 제목·출처·요약을 포함한 행 전체를 원문 링크로 연결해 PDF 링크 클릭 영역 확대
- 별첨 헤더와 첫 기사 사이 간격을 28px, 기사 카드 사이 간격을 5px로 조정해 상단 쏠림 완화
- 2페이지 하단의 snapshot 보존 안내와 확정시각 footer 제거
- 2페이지 타이틀 박스의 기사 요약·원문 이동 설명 문구 제거

기사·브리핑 저장 순서, snapshot, AI 결과, 기사 평가 데이터는 변경하지 않았다. 화면 정렬은
현재 HTML DOM에만 적용되며 `기사 편집순` 버튼으로 원래 순서에 복귀한다. 인쇄는 버튼을 누른
시점의 표시 순서를 따른다.

## 자동 검증

- [x] `.venv/bin/python -m pytest -q` — 378 passed
- [x] `.venv/bin/ruff check .` — 통과
- [x] 렌더러 통합 테스트에서 확대·축소 글자 크기, 축소된 별첨 헤더, 외부 기사 링크,
  정렬 버튼과 정렬 기준 확인

## 수동 회귀

`127.0.0.1:8788` 검증 서버에서 운영일 미리보기를 읽기 전용으로 렌더링했다. 기사 선정·메모·
브리핑 작업본은 변경하지 않았다. 서버 시작 시 기존 startup 작업이 공사 보도자료 캐시를 한 번
갱신했다.

- [x] CEO 미리보기 1페이지의 분석 본문이 기상 특이사항과 같은 14px로 표시된다.
- [x] `오늘 한줄`에는 `CEO VIEW` 라벨이 없고 15.4px 볼드체로 표시된다.
- [x] 2페이지 `관련기사`가 밝고 낮은 별첨 헤더로 표시되며 1페이지보다 시각적 비중이 작다.
- [x] 2페이지의 각 기사 행 전체가 해당 원문 URL을 가진 링크로 렌더링된다.
- [x] 별첨 헤더와 첫 기사 사이 및 기사 카드 사이에 구분 가능한 여백이 표시된다.
- [x] 2페이지 하단에 보존 안내와 확정시각 문구가 표시되지 않는다.
- [x] 2페이지 타이틀 박스에 기사 요약·원문 이동 설명 문구가 표시되지 않는다.
- [x] `기사 중요도순` 클릭 시 중요 표시 → 주의 → 일반 순으로 재배치된다.
- [x] 버튼 문구가 `기사 편집순`으로 바뀌며 다시 클릭하면 원래 편집순으로 복귀한다.
- [x] Chrome 인쇄 미리보기에서 정확히 2쪽으로 표시되고 두 페이지 내용이 잘리지 않는다.
- [x] Chrome PDF 출력 결과가 2쪽이며 실제 기사 URL 링크 주석 15개가 보존된다.
- [x] 정렬 후 인쇄 미리보기에 현재 기사 순서가 반영된다.
- [x] 정렬 버튼 클릭과 페이지 렌더링 과정에서 JavaScript 오류가 발생하지 않는다.

## `MANUAL_REGRESSION_CHECKLIST.md` 관련 결과

- [x] 인쇄 미리보기 열기
- [x] CEO 미리보기 2페이지와 A4 페이지 나눔 확인
- [x] CEO 미리보기에 내부 provider 상태값·오류 원문이 노출되지 않음
- [ ] 기사 선택·해제, 중요 표시 저장, 기사 메모 저장, 날짜 변경, 요약 직접 수정,
  JSON·CSV 내보내기 — 이번 변경이 읽기 전용 보고 렌더러에 한정되어 미실행

## 남은 위험

- 실제 운영일처럼 기사 수가 많은 경우에도 2페이지의 고정 높이·잘림 정책은 기존과 동일하다.
- PDF 링크 보존은 Chrome의 `인쇄·PDF` 출력으로 검증했다. 다른 PDF 프린터 드라이버의 링크
  주석 보존 여부는 해당 드라이버 구현에 좌우된다.
- Chrome Console에서 앱 JavaScript 오류는 없었으나 `favicon.ico` 404가 1건 표시됐다.
  이번 변경과 무관한 기존 정적 리소스 상태이므로 수정하지 않았다.
