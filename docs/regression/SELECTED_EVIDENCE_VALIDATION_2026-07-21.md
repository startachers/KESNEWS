# 선택 기사 근거 유효성 검증 회귀 결과 (2026-07-21)

- 검증 환경: macOS, Chrome, FastAPI 임시 서버 `127.0.0.1:8791`
- 검증 데이터: `/private/tmp/kesco-evidence-ui.db` 전용 픽스처
- 운영 DB·기존 `127.0.0.1:8787` 서버: 변경하지 않음
- 원본 `legacy/kesco_media_briefing_original.html`: 변경하지 않음

## 자동 검증

- `.venv/bin/python -m pytest -q`: 355 passed, 3 dependency deprecation warnings
- `.venv/bin/ruff check .`: 통과
- 변경 프런트 모듈 `node --check`: 통과
- `git diff --check`: 통과

검증 범위에는 출처 정규화·충돌, Google 뉴스 중계 URL, 잘림·오염·미확보,
점수보다 절대 오류 우선, 대표기사 지정 차단, 선택 기사 전체 MD 차단,
자동 교체 없음, 재선택 후 MD 재생성이 포함된다.

## 화면 및 MD 검증

- 관련기사 4건을 펼쳐 최종/최초 언론사, 실제 원문 도메인, 확보 상태,
  품질 점수, 정제 글자 수, 오류 설명을 확인했다.
- 잘림·오염·미확보 기사에서 대표기사/보조근거 버튼이 비활성화되고
  비활성화 사유가 전체 문장으로 표시되는 것을 확인했다.
- 선택 후 상태가 불량해진 대표기사로 MD 생성을 실행했을 때 422
  `SELECTED_EVIDENCE_INVALID`와 함께 전체 생성이 중단되는 것을 확인했다.
- 실패 모달에 기사명, 오류 사유, `관련기사 선택`, `본문 다시 추출`,
  `원문 확인` 작업이 표시되는 것을 확인했다.
- 정상 관련기사로 수동 재선택한 뒤 MD를 다시 생성했다. 결과는
  `selectedCount=1`, `eligibleCount=1`, `replacementCount=0`이며 검증된
  연합뉴스명과 canonical URL만 포함했다.

## 2026-07-20 시나리오 픽스처

- KBS 표시값과 중앙일보 원문 조합: URL·페이지 발행사 근거로 정상화하거나
  상위 근거가 충돌하면 `publisher_identity_mismatch` 처리됨.
- `다만 가정 돌봄이...`: `body_truncated`, 대표기사 지정 불가.
- Copyright/추천기사 뒤 다른 기사 잔존: `body_contaminated`, 대표기사 지정 불가.
- 세 오류가 함께 선택된 경우 세 기사와 각 오류를 모두 반환하고 MD를 생성하지 않음.
- 선택 상태를 자동 삭제·교체하지 않으며 정상 기사 수동 재선택 후 생성 가능.

## 기존 기능 회귀

전체 pytest에서 수집·그룹화·대표/보조근거·분석 제외·메모·중요 표시·날짜별
저장·JSON/CSV·기존 MD·Gemma 근거 검증·최종본/인쇄 관련 기존 테스트가 통과했다.
이번 작업에서는 그룹화, 중복 판단, Gemma 프롬프트, Ollama 호출, 인쇄 CSS를
수정하지 않았다. 전체 수동 체크리스트의 네트워크 공급자 수집과 실제 인쇄물
육안 비교는 외부 환경 의존 항목으로 이번 임시 픽스처 검증에서 재실행하지 않았다.
