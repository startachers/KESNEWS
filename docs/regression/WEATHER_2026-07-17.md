# 기상 기반 선제대응 회귀 결과 — 2026-07-17

## 범위

- 기상청 단기·중기예보와 특보 수집
- provider 장애 시 마지막 정상 context 보존
- 7일(D0~D6) 전국·6권역 요약
- 담당자 검토·신호 override·CEO 보고 snapshot
- Gemma `Wxx` 근거 분리와 템플릿 fallback
- JSON schemaVersion 12 왕복
- 앱 시작 및 2시간 주기 launchd 자동수집

## 자동 검증

- `python -m pytest -q`: **302 passed**, 기존 deprecation warning 3건
- `ruff check .`: **통과**
- 전체 `frontend/js/*.js` `node --check`: **통과**
- `git diff --check`: **통과**
- 기상청 실호출: alerts·shortForecast·midForecast 모두 **success**
- launchd 첫 실행에서 청주 단기예보 HTTP 502 부분 실패를 관측했고, 해당 권역의 마지막 정상
  요약을 재사용하면서 provider 상태와 오류는 `partial`로 유지하는 테스트를 추가함
- 최신 context: 2026-07-17~2026-07-23, 7일, 날짜별 6권역 요약, 위험 신호 3건
- `GET /api/weather/runs/{run_id}`: provider 3건 상세 응답 확인
- launchd: server·collection·weather 세 LaunchAgent 등록 확인
- 재시작 후 health: `ok=true`, `dbConnected=true`, `dbIntegrity=true`

## 화면 확인

- Chrome headless 1440×1400에서 편집 화면 렌더링 확인
- 메인 화면은 `기상 특이사항`의 상태·특보·주요 예보와 핵심 판단 수치만 표시하고 상세 정보·관리 기능은 모달로 분리
- 메인 요약에 7일 최저·최고기온, 단기예보 최대 시간강수량, 최고 강수확률과 항목별 SVG 아이콘 추가
- 복수 현상이 한 특보 본문에 섞일 때 현상별 행으로 분리하여 단계·권역을 각각 계산하고, 메인에 영향 권역과 최고 위험 시·군을 표시
- 상세 버튼으로 모달이 열리고 배경 스크롤이 잠기며, Escape로 닫힌 뒤 스크롤 잠금이 해제됨을 확인
- 상세 모달의 전국·6권역 선택, 7일 카드, 검토·새로고침 버튼의 중첩과 가로 넘침 없음
- 상세 모달을 판단 근거 화면으로 확장: 핵심 위험 수치, provider 최신성, 공식 특보 원문 6건,
  날짜별 권역 비교 6건, 전체 위험 신호 5건과 공식 근거·발효시각 렌더링 확인
- 상세 정보를 `종합판단`·`특보·대응`·`권역비교` 탭으로 분리하고 탭별 단일 panel 노출 확인
- 공식 단계·수치 임계값·권역 내 최댓값 비교 강조 확인: 고위험 일자 2개, 주의 일자 4개,
  권역비교 고위험 1개·주의 3개·일반 2개로 선별 표시
- 기상 section, 7일 카드, 전국·6권역 선택, 위험도 텍스트, 위험 신호 3건 확인
- 각 위험 신호의 보고 반영 체크·담당자 단계·메모 컨트롤 확인
- 편집 화면의 가로 넘침과 컨트롤 중첩 없음
- 모든 JS/CSS/module 요청 200 확인. favicon 404 외 앱 리소스 오류 없음

## `MANUAL_REGRESSION_CHECKLIST.md` 결과

- 기사 선택·해제, 중요 표시, 기사 메모, 날짜 변경, 요약 직접 수정, JSON·CSV, 최종 snapshot:
  전체 통합 테스트로 회귀 확인
- 기상 정상·부분 실패·stale·unknown: 단위·통합 테스트 통과
- 보고 반영 전/후, 최신 context 경고, 담당자 override 보존: 통합 테스트 통과
- JSON 내보내기→가져오기 기상 round-trip: 통합 테스트 통과
- 실제 화면 권역 전환 클릭: in-app browser 실행 도구 미연결로 미실행. 권역 데이터 응답과
  선택 UI 렌더링 및 JS 구문은 확인
- 실제 인쇄 대화상자와 A4 실물 출력: 미실행. preview/final renderer 통합 테스트 통과
- 실제 Gemma 기상 문장 생성: 장시간 모델 실행은 미실행. `Wxx` schema·서명·fallback 단위
  테스트 통과

## 남은 운영 확인

- 다음 2시간 주기 weather LaunchAgent의 첫 자연 실행 시각과 `logs/weather.log` 기록 확인
- 다음 실제 CEO 보고에서 담당자 기상 검토 후 미리보기·A4 인쇄 육안 확인
